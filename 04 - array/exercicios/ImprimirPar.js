/*
percorrer umas lista e mostrar cada numero par encontrado:
*/

const lista = [5, 6 , 8, 55, 9, 10, 72, 7, 33, 32];

for(let i = 0; i < lista.length; i++) {
    if(lista[i] % 2 === 0) {
        console.log(lista[i]);
    } else {

    }
}

const listaDeValores = [1, 2, 3, 4, 5];
const valor = listaDeValores[2];
console.log(valor);